return {
  {
    "scrooloose/nerdcommenter",
    -- Trying to set keymaps here but doesn't work. They are in keymaps.lua instead
    --init = function()
    --vim.g.NERDCreateDefaultMappings = 1 -- enable default mappings but remap them
    ----vim.api.nvim_set_keymap("n", "<C-_>", "<leader>c<Space>", {})
    ----vim.api.nvim_set_keymap("v", "<C-_>", "<leader>c<Space>", {})
    --end,
    --keys = {
    --{ "<C-_>", "<cmd>call nerdcommenter#Comment('x', 'toggle')<CR>", mode = { "n" } },
    ----{ "<C-_>", "<cmd>call nerdcommenter#Comment('x', 'toggle')<CR>", mode = { "v" } },
    --},
  },
}
